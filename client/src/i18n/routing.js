import { LOCALES, DEFAULT_LOCALE } from './index';

// Mirrors the shape the components expect from the old next-intl routing object.
export const routing = {
  locales: LOCALES,
  defaultLocale: DEFAULT_LOCALE,
};
